# DocRaptor C# Native Client Library

This is a fork of the parent library which removes unused (by me) code and makes it .NET Standard compatible.
It's a snapshot and won't be kept up-to-date with the upstream.